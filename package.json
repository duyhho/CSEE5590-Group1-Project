{
  "name": "CSEE5590-Group1-Project",
  "version": "1.0.0",
  "dependencies": {},
  "scripts": {
    "install": "(cd backend && npm install); (cd breakout && npm install)",
    "start": "npm-run-all --parallel startBackEnd startFrontEnd",
    "startBackEnd": "npm run start --prefix backend/",
    "startFrontEnd": "npm run start --prefix breakout/"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
